{
  "name": "random",
  "version": "1.0.0",
  "scripts": {
    "start": "ts-node src/bot.ts",
    "build": "tsc",
    "ts-node": "ts-node",
    "ready": "npx prisma generate && npx prisma migrate dev",
    "prisma-generate": "npx prisma generate",
    "prisma-migrate": "npx prisma migrate dev"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ipmanlk/random.git"
  },
  "devDependencies": {
    "@prisma/client": "^4.9.0",
    "@types/express": "^4.17.16",
    "@types/node": "^18.11.18",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "express": "^4.18.2",
    "prisma": "^4.9.0"
  }
}
